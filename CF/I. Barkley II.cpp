#include <bits/stdc++.h>

using namespace std;

void solve() {
    int n, m;
    cin >> n >> m;
    vector<int> p(n);
    for(int i = 0; i < n;i ++) 
        cin >> p[i];
    
}

signed main() {
    int T;
    cin >> T;
    while(T--)
        solve();
}