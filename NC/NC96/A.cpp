#include <iostream>
#include <algorithm>
#include <vector>
#include <queue>
#include <map>
#include <set>
#include <map>
#include <deque>

using namespace std;

#define x first
#define y second
#define endl '\n'
// #define int long long

using PII = pair<int, int>;

using LL = long long;

void solve() {
    int x;
    cin >> x;
    cout << x * 8 << endl; 
}

signed main() {
    ios::sync_with_stdio (false);
    cin.tie(nullptr);
    // int T;cin >> T;
    int T = 1;
    while(T --> 0) {
        solve();
    }
}